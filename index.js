var myPhoneNumber=95435435
var myName ="the man";

console.log(isNaN(myPhoneNumber));
console.log(isNaN(myName));
 // if can we this case for check the data is number or not 
 // in this code :if we put  myName-- the out will be the plese enter the valid number and   vice versa 
if(isNaN(myPhoneNumber)){
    console.log("please enter valid phone number");
    
}

